# Map Components

This directory provides the target structure alias for map-focused components.

Current live implementations are re-exported from `frontend/src/components/threat-map/` to avoid breaking existing imports while aligning the project layout for presentation and future cleanup.
