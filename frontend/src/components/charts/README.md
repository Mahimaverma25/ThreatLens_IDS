# Chart Components

Use this folder for reusable chart blocks extracted from dashboard pages.

The project currently still renders most charts directly inside page-level components, but this directory now exists as the intended shared location for future chart refactors.
